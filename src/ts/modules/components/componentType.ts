export enum ComponentType {
    DEFAULT,
    CLASS,
    ENUM,
    INTERFACE,
    PRIMITIVE,
    DATA_TYPE,
    NOTE,
    GENERALIZATION,
    USAGE,
    ASSOCIATION,
    AGGREGATION,
    COMPOSITION,
}
